public class Constants {
    // Name of the topic
    public static final String TOPIC_NAME = "SimpleAggr";
    // The interval between message publications by the root (in milliseconds)
    public static final int PUBLISH_INTERVAL = 5000;
    // Number of nodes in the system for different tests
    public static final int[] NODE_COUNTS = {10, 30, 1000};
}
